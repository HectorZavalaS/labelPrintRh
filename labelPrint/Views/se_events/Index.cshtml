@model IEnumerable<labelPrint.Models.siixseem_events_t>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.se_description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.se_description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.se_id_event }) |
            @Html.ActionLink("Details", "Details", new { id=item.se_id_event }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.se_id_event })
        </td>
    </tr>
}

</table>
