@model IEnumerable<labelPrint.Models.siixsem_densities_t>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.se_density)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.se_density)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.se_id_density }) |
            @Html.ActionLink("Details", "Details", new { id=item.se_id_density }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.se_id_density })
        </td>
    </tr>
}

</table>
