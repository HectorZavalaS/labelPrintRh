@model IEnumerable<labelPrint.Models.siixsem_lblSizes_t>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.se_description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.se_height)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.se_width)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.se_description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.se_height)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.se_width)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.se_id_size }) |
            @Html.ActionLink("Details", "Details", new { id=item.se_id_size }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.se_id_size })
        </td>
    </tr>
}

</table>
